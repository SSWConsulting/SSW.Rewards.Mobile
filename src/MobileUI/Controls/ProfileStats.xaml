<?xml version="1.0" encoding="utf-8" ?>
<Border 
    HeightRequest="260"
    BackgroundColor="{StaticResource MainBackground}"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Class="SSW.Rewards.Mobile.Controls.ProfileStats">
    <Border.Resources>
        <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
    </Border.Resources>
    <Border.StrokeShape>
        <RoundRectangle CornerRadius="8" />
    </Border.StrokeShape>
    <Grid 
        RowDefinitions="120, 40, 65"
        RowSpacing="10">
        <VerticalStackLayout Grid.Row="0">
            <Image
                Source="{Binding ProfilePic}"
                WidthRequest="120"
                HeightRequest="120"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Aspect="AspectFill">
                <Image.Clip>
                    <EllipseGeometry
                        Center="60,60"
                        RadiusX="60"
                        RadiusY="60"/>
                </Image.Clip>
            </Image>

            <Border
                BackgroundColor="{StaticResource primary}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                TranslationX="40"
                TranslationY="-35"
                StrokeShape="Ellipse"
                StrokeThickness="0"
                IsVisible="{Binding IsMe}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CameraCommand}"/>
                </Border.GestureRecognizers>
                <Label
                    FontFamily="FluentIcons"
                    FontSize="25"
                    Text="&#xe299;"
                    Margin="3"
                    TextColor="White"/>
            </Border>
        </VerticalStackLayout>
        
        <VerticalStackLayout Grid.Row="1"
        >
        <Label
            HorizontalOptions="Center"
            VerticalOptions="Center"
            HorizontalTextAlignment="Center"
            Text="{Binding Name}"
            FontAttributes="Bold"
            FontSize="Large"
            TextColor="White"/>    
        <Label
            HorizontalOptions="Center"
            VerticalOptions="Center"
            HorizontalTextAlignment="Center"
            Text="SSW"
            FontAttributes="Bold"
            FontSize="Small"
            TextColor="White"/>
        </VerticalStackLayout>
        
        <!-- User's own stats -->
        <Border Grid.Row="2"
                IsVisible="{Binding Path=ShowBalance}"
                Stroke="{StaticResource ProfileGridBorder}"
                StrokeThickness="2"
                Padding="0,10"
                Margin="10, 0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <Grid 
                HorizontalOptions="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <VerticalStackLayout Grid.Column="0"
                                     Spacing="2"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center">
                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                        <Label VerticalOptions="Center"
                               FontFamily="FA6Solid"
                               TextColor="{StaticResource Coin}"
                               Text="&#xf091;"/> 
                        <Label FontSize="Large" FontAttributes="Bold" Text="0001"/>
                    </HorizontalStackLayout>
                    <Label
                        HorizontalOptions="Center"
                        Text="Ranking"
                        FontSize="Small"
                        TextColor="{StaticResource White}" />  
                </VerticalStackLayout>  
                <VerticalStackLayout Grid.Column="1"
                                     Spacing="2"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center">
                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                        <Label VerticalOptions="Center"
                               FontFamily="FA6Solid"
                               TextColor="{StaticResource Coin}"
                               Text="&#x2b50;"/> 
                        <Label FontSize="Large" FontAttributes="Bold" Text="{Binding Points, StringFormat='{0:n0}'}"/>
                    </HorizontalStackLayout>
                    <Label
                        HorizontalOptions="Center"
                        Text="Points"
                        FontSize="Small"
                        TextColor="{StaticResource White}" />  
                </VerticalStackLayout>       
                <VerticalStackLayout Grid.Column="2"
                                     Spacing="2"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center">
                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                        <Label VerticalOptions="Center"
                               VerticalTextAlignment="Center"
                               FontFamily="FA6Solid"
                               TextColor="{StaticResource Coin}"
                               Text="&#xf51e;"/> 
                        <Label FontSize="Large" FontAttributes="Bold" Text="{Binding Balance, StringFormat='{0:n0}'}"/>
                    </HorizontalStackLayout>
                    <Label
                        HorizontalTextAlignment="Center"
                        HorizontalOptions="Center"
                        Text="Credits"
                        FontSize="Small"
                        TextColor="{StaticResource White}" />  
                </VerticalStackLayout>
            </Grid>
        </Border>
        
        <!-- Viewing other User's stats -->
        <Border Grid.Row="2"
        Stroke="{StaticResource ProfileGridBorder}"
        StrokeThickness="2"
        Padding="0,10"
        Margin="10, 0"
        IsVisible="{Binding Converter={StaticResource InvertedBoolConverter}, Path=ShowBalance}"
        >
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <Grid 
            ColumnDefinitions="150,150"
            HorizontalOptions="Center">
                <VerticalStackLayout Grid.Column="0"
                                     Spacing="2"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center">
                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                        <Label VerticalOptions="Center"
                               FontFamily="FA6Solid"
                               TextColor="{StaticResource Coin}"
                               Text="&#xf091;"/> 
                        <Label FontSize="Large" FontAttributes="Bold" Text="0001"/>
                    </HorizontalStackLayout>
                    <Label
                        HorizontalOptions="Center"
                        Text="Ranking"
                        FontSize="Small"
                        TextColor="{StaticResource White}" />  
                </VerticalStackLayout>  
                <VerticalStackLayout Grid.Column="1"
                                     Spacing="2"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center">
                    <HorizontalStackLayout Spacing="5" HorizontalOptions="Center">
                        <Label VerticalOptions="Center"
                               FontFamily="FA6Solid"
                               TextColor="{StaticResource Coin}"
                               Text="&#x2b50;"/> 
                        <Label FontSize="Large" FontAttributes="Bold" Text="{Binding Points, StringFormat='{0:n0}'}"/>
                    </HorizontalStackLayout>
                    <Label
                        HorizontalOptions="Center"
                        Text="Points"
                        FontSize="Small"
                        TextColor="{StaticResource White}" />  
                </VerticalStackLayout>       
            </Grid>
        </Border>
    </Grid>
</Border>

