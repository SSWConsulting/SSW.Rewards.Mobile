<?xml version="1.0" encoding="utf-8"?>

<Border
    BackgroundColor="{StaticResource Background}"
    Stroke="{StaticResource Background}"
    StrokeShape="RoundRectangle 8"
    Padding="15"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:SSW.Rewards.Mobile.Controls"
    xmlns:viewModels="clr-namespace:SSW.Rewards.Mobile.ViewModels"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Class="SSW.Rewards.Mobile.Controls.ProfileActivity"
    x:DataType="viewModels:ProfileViewModelBase">
    <VerticalStackLayout>
        <Label Text="Recent Activity"
               FontSize="16"
               Style="{StaticResource LabelBold}"
               Margin="0, 5" />

        <VerticalStackLayout
            BindableLayout.ItemsSource="{Binding Activity}"
            Spacing="8">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <controls:ProfileActivityItem />
                </DataTemplate>
            </BindableLayout.ItemTemplate>
            <BindableLayout.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" Spacing="10" Margin="0, 10">
                    <Label Text="No activity"
                           TextColor="{StaticResource LeaderCardBackground}"
                           HorizontalOptions="Center"
                           IsVisible="{Binding IsMe, Converter={toolkit:InvertedBoolConverter}}" />
                    <Button Text="Start earning points by scanning QR codes"
                            Command="{Binding GoToScanPageCommand}"
                            IsVisible="{Binding IsMe}"
                            HorizontalOptions="Center" />
                </VerticalStackLayout>
            </BindableLayout.EmptyView>
        </VerticalStackLayout>
    </VerticalStackLayout>
</Border>