<!-- TEMPLATE — real values come from Keeper, written by the Aspire "Mobile: Materialize Firebase secrets"
     command (or copy manually). The real GoogleService-Info.plist is git-ignored. Do not commit real values. -->
