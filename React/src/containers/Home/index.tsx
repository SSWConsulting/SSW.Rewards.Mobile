import React from "react";

import { Typography } from "@material-ui/core";

const Home = (): JSX.Element => {
 return(
   <Typography> Welcome to SSW Rewards Admin</Typography>
 )
};

export default Home;
